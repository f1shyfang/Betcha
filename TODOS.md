# TODOS — Betcha

## P0 — Pilot-Blocking (fix before Day 7 invitations)

### Fix invite flow: shareable link + /join route
**What:** (1) Replace raw UUID token + "POST /api/groups/join" in groups/[id].js invite banner with a full shareable URL (`window.location.origin + '/join?token=' + token`) and copy button. (2) Create `pages/join.js` — if not logged in, show "Join Betcha to accept this invite" with signup/login links preserving the token in URL; if logged in, auto-call POST /api/groups/join and redirect to the group.
**Why:** The invite mechanic is the only way friends join a group. Currently the invite banner shows an API endpoint description, not a usable link. Pilot invitations will fail.
**Context:** Invite token is already generated by POST /api/groups/[id]/invites. The join API (POST /api/groups/join) already works. This is purely the UI+route layer.
**Effort:** S (CC ~20 min)
**Priority:** P0 — pilot fails without this
**Depends on:** Existing POST /api/groups/join endpoint

---

### Leaderboard: show display name or email, not UUID fragment
**What:** Fix groups/[id].js leaderboard to show recognizable user identifiers instead of `entry.user_id.substring(0, 4)`. Update `/api/groups/[id]/leaderboard` to JOIN users table and return `email` or `display_name` alongside score.
**Why:** The leaderboard is the social engagement mechanic. "User a3f8" has no meaning to anyone. Pilot participants need to see "Sam", "alex@gmail" etc. to feel competitive.
**Context:** Supabase auth users have email. A users table with display_name is the right long-term solution. For the pilot, email-prefix (before @) is sufficient.
**Effort:** S (CC ~15 min — one SQL JOIN in the leaderboard query + 2 line change in the component)
**Priority:** P0 — leaderboard is decorative without real names
**Depends on:** /api/groups/[id]/leaderboard endpoint

---

### Use existing leaderboard CSS classes in groups/[id].js
**What:** Replace the ad-hoc `.cards` grid used for the leaderboard in groups/[id].js (lines ~183-190) with the existing `.leaderboard-section` and `.leaderboard-row` CSS classes defined in globals.css (lines 844-916).
**Why:** The correct leaderboard styling already exists in the CSS. The component just isn't using it.
**Context:** globals.css has `.leaderboard-section` (dark surface, white text), `.leaderboard-row`, `.leaderboard-rank`, `.leaderboard-name`, `.leaderboard-result`, `.result-won`, `.result-lost`.
**Effort:** XS (CC ~5 min)
**Priority:** P0 — visual polish matters for pilot trust

---

### Reorder group dashboard: content first, create second
**What:** In groups/[id].js, move the "Live Markets" section and "Leaderboard" section ABOVE the "Create New Market" form. Add member count and group context below the group name in the header.
**Why:** Users who join via invite should see social activity immediately. Currently they see a blank creation form.
**Context:** This is a JSX reorder — move the `<section>` blocks, no logic changes needed.
**Effort:** XS (CC ~5 min)
**Priority:** P0 — affects every invited user's first impression

---

### Hide resolve controls from non-creators
**What:** In markets/[id].js, wrap the resolve section in a conditional: only show if `currentUser.id === market.creator_id`. Thread `currentUser` through the component using `supabase.auth.getSession()` (already called in fetchMarket).
**Why:** Every user currently sees "Creator Actions: Resolve Market" with YES/NO buttons. Non-creators clicking resolve either fails silently or triggers a dispute.
**Context:** The market object needs `creator_id` — verify the `/api/markets` response includes it. If not, add it to the SELECT in the markets API.
**Effort:** S (CC ~10 min)
**Priority:** P0 — trust issue in every market

---

### Show user's own prediction after voting
**What:** In markets/[id].js, filter the predictions array for `p.user_id === currentUser.id` and display "Your prediction: YES / NO" below the odds bar. Dim the unchosen button when a prediction exists.
**Why:** After voting, there's no confirmation that the vote registered. Users can't tell if they've voted.
**Context:** Predictions are already fetched every 5 seconds. Just filter for currentUser.id. currentUser is available via supabase.auth.getSession() already called in the component.
**Effort:** S (CC ~10 min)
**Priority:** P0 — creates anxiety and potential double-votes in pilot

---

### Auto-redirect after login and signup
**What:** In login.js, after successful sign-in, call `router.push('/groups')` after a 500ms delay. In signup.js, after successful sign-up, call `router.push('/login?created=true')`. In login.js, show "Account created — please log in" if `router.query.created === 'true'`.
**Why:** Currently login shows "Logged in successfully." and the user stays on the login page. Signup says "You can now login" with no link or redirect.
**Context:** `useRouter` is already imported in both pages.
**Effort:** XS (CC ~5 min each)
**Priority:** P0 — first impression of product quality

---

## P1 — Pilot Quality (fix in first week)

### Resolution moment: winners + score delta + share
**What:** Replace the bare "Outcome: YES/NO" banner in markets/[id].js with: (1) large outcome display, (2) list of correct predictors (from predictions array filtered by resolution outcome), (3) score delta for current user ("You earned +5 points"), (4) share button (navigator.share or copy-to-clipboard of market title + outcome).
**Why:** The resolution is the product's core emotional payoff. "Outcome: NO" lands completely flat.
**Context:** All the data is available: predictions (already fetched), resolution outcome (in market.resolution.outcome), leaderboard for score delta (can be derived). Share uses the Web Share API with a text fallback.
**Effort:** M (CC ~30 min)
**Priority:** P1 — affects pilot retention; doesn't block invitations

---

### Skeleton loading states for all 4 app pages
**What:** Replace `<div className="page">Loading...</div>` in groups/index.js, groups/[id].js, markets/index.js, markets/[id].js with skeleton shimmer placeholder states.
**Why:** Bare loading text looks unfinished. Every page transition shows it.
**Context:** globals.css already has float-in and rise-in animations. Add a `.skeleton-shimmer` animation (~15 lines CSS) and swap loading renders to show placeholder shapes matching each page's layout.
**Effort:** M (CC ~30 min)
**Priority:** P1 — polish matters for pilot trust

---

### Replace two 3-column feature grids on landing page
**What:** Remove the `.cards` section (lines 241-253, "Accountability-first / Invite-only groups / Fast to start") and `.use-cases` section (lines 255-268, "Accountability / Fun / Rituals") from index.js. Replace with a single, differentiated section — either a horizontal scrolling ticker of example market questions or a large-type statement block.
**Why:** Two back-to-back 3-column grids are the most recognizable AI-generated layout pattern. They undercut the strong hero.
**Context:** Example ticker copy: "Will Sam gym 4x this week? · Who wins Friday trivia? · Does the apartment stay clean til Sunday? ·" — real Betcha questions make the product feel specific.
**Effort:** M (CC ~45 min)
**Priority:** P1 — landing page polish; doesn't block pilot invitations

---

### Remove Quick Create from landing page
**What:** Remove the `<section className="quick-create-section">` block (lines 194-237) from pages/index.js. It asks unauthenticated visitors for a "Group ID (optional) — Paste a group ID from your dashboard."
**Why:** Public landing visitors have no group ID. The form doesn't work without auth anyway.
**Context:** Quick Create already exists in groups/[id].js for authenticated users.
**Effort:** XS (CC ~2 min — just delete the JSX block and remove the related state vars)
**Priority:** P1 — cleanup; quick-create in app is the right placement

---

## P2 — Post-Launch

### pages/markets/[id].js — direct market fetch
**What:** Replace the all-markets fetch in `pages/markets/[id].js` with a direct `GET /api/markets/:id` call.  
**Why:** Currently fetches all user's markets across all groups and filters client-side. The direct endpoint already exists. Fine at MVP scale, breaks at 100+ markets.  
**Context:** `pages/api/markets/[id]/index.js` is the target endpoint. `lib/api.js:getMarket()` is the client wrapper. Fix is ~5 lines in the page component.  
**Effort:** S (CC ~5 min)  
**Priority:** P2 — fix before any marketing or public sharing  

---

### Season / recurring markets
**What:** Add recurring "season" mechanics — a 12-week accountability loop with weekly auto-resolve and streak tracking.  
**Why:** The design doc (Approach C) identified this as the retention differentiator. The trigger signal: if 1+ group requests a weekly accountability market template by day 14.  
**Context:** Current one-off market model is Approach A (validated first). Seasons are the next phase. Requires: recurring market schema, cron-based resolution, streak ledger.  
**Effort:** L (CC ~2 hrs)  
**Priority:** P2 — revisit after day-14 success metrics. Upgrade to P1 if a group explicitly requests recurring markets.  
**Depends on:** At least 3 resolved markets in 2-3 friend groups by day 14.  

---

### Comprehensive test suite
**What:** Unit/integration tests for groups API, markets API, predictions API, invites flow, auth middleware, leaderboard calculation.  
**Why:** The entire API surface (except the resolve integration test added in fix-and-ship) has zero automated coverage. Currently relying on manual testing.  
**Context:** Use `test/database-url.test.js` as the pattern (direct Supabase REST calls). Each API handler needs: happy path, auth failure (401), forbidden (403), not-found (404), and the key error paths from the Error & Rescue Map.  
**Effort:** M (CC ~30 min)  
**Priority:** P2 — not blocking launch. Becomes expensive to add after codebase grows 2-3x.  
